export const MyDivider = () => {
    return (
        <div style={styles}></div>
    )
}

const styles = {
    border: '1px solid rgba(0, 0, 0, 0.54)',
    width: '20px',
    transform: 'rotate(90deg)'
}